# Local Demo mode: no scheduled Render ping.
name: Wake Render manually (backup)

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check
        run: curl -sf --max-time 60 https://matcha-shopbeta.onrender.com/healthz/
