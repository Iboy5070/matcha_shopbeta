# Local Demo mode: Render wake-up is available manually only.
name: Wake Render manually

on:
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping health check
        run: curl -sf --max-time 60 https://matcha-shopbeta.onrender.com/healthz/
